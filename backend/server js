const express = require('express')
const cors = require('cors')
const dotenv = require('dotenv')

dotenv.config()

const app = express()

app.use(cors())
app.use(express.json())

app.get('/', (req, res) => {
  res.json({ message: 'Souci kitchen is open!' })
})

const PORT = 4000

app.listen(PORT, () => {
  console.log(`Kitchen is running on door ${PORT}`)
})